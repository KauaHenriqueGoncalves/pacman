<Page
    x:Class="CapstonePacMan.Presentation.GameOverPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CapstonePacMan.Presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <local:FinishGameViewModel/>
    </Page.DataContext>

    <Grid>
        <Border Background="#FF111111"
                CornerRadius="20"
                Padding="30"
                BorderBrush="Red"
                BorderThickness="3"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Width="360">

            <StackPanel Spacing="20">

                <TextBlock Text="GAME OVER"
                           FontSize="36"
                           FontWeight="Bold"
                           Foreground="Red"
                           HorizontalAlignment="Center"/>

                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Sua pontuação"
                               FontSize="16"
                               Foreground="White"
                               HorizontalAlignment="Center"/>

                    <TextBlock Text="{Binding Pontos}"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="DarkRed"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="Digite seu nome:"
                           Foreground="White"
                           FontSize="14"
                           HorizontalAlignment="Center"/>

                <TextBox Width="220"
                         HorizontalAlignment="Center"
                         Text="{Binding NomeJogador, Mode=TwoWay}"
                         PlaceholderText="Seu nome"
                         FontSize="14"/>

                <StackPanel Spacing="10">

                    <Button Content="Salvar no Ranking"
                            Command="{Binding SalvarCommand}"
                            Background="Yellow"
                            Foreground="Black"
                            FontWeight="Bold"
                            Height="45"
                            CornerRadius="10"
                            HorizontalAlignment="Center"/>

                    <Button Content="Reiniciar"
                            Command="{Binding ReiniciarCommand}"
                            Background="#FF333333"
                            Foreground="White"
                            FontSize="14"
                            Height="40"
                            CornerRadius="10"
                            HorizontalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>

